export type PortfolioItem = {
  id: string;
  title: string;
  description: string;
  imageId: string;
};

export type BlogPost = {
  id: string;
  title:string;
  description: string;
  imageId: string;
  author: string;
  date: string;
};
